import React, { Component } from "react";
import Icon from "material-ui/Icon";

class RepeatIcon extends Component {
    render() {
        return (
            <Icon className="fa fa-repeat" {...this.props} />
        );
    }
}

export default RepeatIcon;
